(if (and
        (is (window_role) "conversation")
        (is (window_class) "Pidgin"))
    (begin
        (stick)
        (skip_pager)
        (skip_tasklist)
        ; (opacity 100) ; works w/ compositing only
    )
)
